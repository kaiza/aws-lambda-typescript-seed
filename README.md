# aws-lambda-typescript-seed

Seed project for building AWS Lambda functions using typescript.

## Getting Started

`npm install`

## Build/Deploy

Run `npm run build` then upload function to AWS Lambda.